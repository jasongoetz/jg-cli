{
  "name": "jg-cli",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^10.0.0",
    "typescript": "^4.5.4",
    "yargs": "^17.3.1"
  },
  "scripts": {
    "clean": "rm -rf lib && tsc --build --clean && yarn",
    "build": "tsc --build"
  },
  "devDependencies": {
    "@types/node": "^17.0.5",
    "@types/yargs": "^17.0.8"
  }
}
